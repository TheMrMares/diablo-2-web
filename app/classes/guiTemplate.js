export class guiTemplate {
    constructor(drawground, name, buttons){
        this.drawground = drawground;
        this.name = name;
        this.objects = {
            buttons: buttons
        };
    }
}